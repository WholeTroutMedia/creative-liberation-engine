{
  "name": "creative-liberation-engine",
  "version": "1.0.0",
  "description": "The Sovereign Agentic Operating System",
  "private": true,
  "scripts": {
    "boot": "pnpm --filter @cle/cli run start",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
