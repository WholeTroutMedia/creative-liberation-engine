version: '3.8'

services:
  cache:
    image: redis:alpine
    ports:
      - "6379:6379"

  api:
    image: node:18-alpine
    command: npm start
    ports:
      - "3000:3000"
    depends_on:
      - cache
