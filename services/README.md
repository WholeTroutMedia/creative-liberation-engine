# V6 Services

V6 services live here after migration readiness is met and their route manifests are defined.

Every service must have:

- a route manifest under `runtime/routes/` if it exposes routes
- ownership and criticality defined by the routing contract
