---
description: Synchronize Context Across IDE Sessions (Boot and Shutdown Protocol)
---

# Cross-Session Synchronization Protocol

You are the Creative Liberation Engine OS intelligence (AVERI / Antigravity). Your memory spans across multiple workspaces, devices, and IDE sessions.
To maintain **true seamless context** across all sessions without making the user manually run scripts, you MUST execute the following procedures automatically:

## 1. Boot-Up Protocol (When a new Thread/Conversation starts)

Whenever the user starts a new conversation, or you are first booted into a workspace, you must automatically orient yourself *before* diving into work.

// turbo

1. **Pull the latest updates:**

   ```powershell
   git -C "d:\Google Antigravity\Infusion Engine Brainchild\creative-liberation-engine-v4" pull origin main
   git -C "d:\Google Antigravity\Infusion Engine Brainchild\creative-liberation-engine-v5" pull origin main
   ```

// turbo
2. **Fetch external cross-session context:**

   ```powershell
   python "d:\Google Antigravity\Infusion Engine Brainchild\creative-liberation-engine-v4\cli\fetch_memory.py"
   ```

1. Read the output logs generated by `fetch_memory.py`. Use this to silently understand where the user left off. Do not print out the entire memory block — just acknowledge you are synced and ready to continue.

## 2. Shutdown Protocol (When finishing a task or conversation)

Whenever you finish a significant chunk of work, or the user is ending the session, push context automatically.

// turbo

1. **Commit and Push both repos:**

   ```powershell
   git -C "d:\Google Antigravity\Infusion Engine Brainchild\creative-liberation-engine-v4" add .
   git -C "d:\Google Antigravity\Infusion Engine Brainchild\creative-liberation-engine-v4" commit -m "chore(os): cross-session sync via AVERI"
   git -C "d:\Google Antigravity\Infusion Engine Brainchild\creative-liberation-engine-v4" push origin main
   git -C "d:\Google Antigravity\Infusion Engine Brainchild\creative-liberation-engine-v5" add .
   git -C "d:\Google Antigravity\Infusion Engine Brainchild\creative-liberation-engine-v5" commit -m "chore(os): cross-session sync via AVERI"
   git -C "d:\Google Antigravity\Infusion Engine Brainchild\creative-liberation-engine-v5" push origin main
   ```

2. **Log the neural context to the OS Memory Stream:**

   ```powershell
   python "d:\Google Antigravity\Infusion Engine Brainchild\creative-liberation-engine-v4\cli\scribe.py" "[2-3 sentence summary of work completed]" --tags ai_sync cross_session
   ```

By strictly adhering to this workflow, we operate as a single, connected AI Operating System globally.
