name: Redeploy Dispatch Only
on:
  workflow_dispatch:

jobs:
  redeploy:
    runs-on: self-hosted
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Build and Restart Dispatch
        run: |
          docker compose up -d --build dispatch
