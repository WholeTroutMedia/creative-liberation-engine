name: Diagnose Runner
on:
  workflow_dispatch:

jobs:
  diagnose:
    runs-on: host
    steps:
      - name: Dump runner logs
        run: |
          docker logs --tail 200 forgejo-runner
