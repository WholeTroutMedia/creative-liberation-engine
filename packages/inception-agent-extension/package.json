{
  "name": "@cle/agent-extension",
  "version": "2.0.3",
  "description": "Creative Liberation Engine browser agent \u2014 MV3 Chrome/Arc/Opera/Firefox extension",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "build:firefox": "cross-env TARGET=firefox vite build",
    "build:all": "npm run build && npm run build:firefox",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests defined\""
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "cross-env": "^7.0.3",
    "jsdom": "^28.1.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vitest": "^4.0.18"
  }
}
