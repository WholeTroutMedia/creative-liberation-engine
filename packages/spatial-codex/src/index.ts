export async function ingestHardwareSpec(query: string): Promise<any> {
    console.log(`[spatial-codex] ingestHardwareSpec called for: ${query}`);
    return {};
}

export function searchHardwareCodex(query: string): any[] {
    console.log(`[spatial-codex] searchHardwareCodex called for: ${query}`);
    return [];
}
